{
  "name": "@f/defer-promise",
  "description": "Like Promise.defer().",
  "repository": "git://github.com/micro-js/defer-promise.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
